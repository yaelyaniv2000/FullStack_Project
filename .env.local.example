# Copy this file to .env.local and fill in real values from your Supabase project
# (Project Settings -> API). Never commit .env.local itself.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Added later, server-only, needed only for the admin worker-invite-by-email flow.
# Never expose this one to the browser (no NEXT_PUBLIC_ prefix) and never commit it.
# SUPABASE_SERVICE_ROLE_KEY=